[package]
name = "bdk_coin_select"
version = "0.1.0"
edition = "2018"
license = "MIT OR Apache-2.0"

[dependencies]
# No dependencies! Don't add any please!

[dev-dependencies]
rand = "0.7"
proptest = "0.10"
bitcoin = "0.30"

[features]
default = ["std"]
std = []
